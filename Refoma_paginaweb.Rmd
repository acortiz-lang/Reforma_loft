---
title: "Reforma Loft"
author: "Arq. Ana Ortiz"
date: "`r Sys.Date()`"
output:
  word_document: default
  html_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.



## Mi primer pagina web

Esta es mi primer pagina web y voy a hecr una prueba de como hacer una reforma de un loft.

Los pasos que he seguido son:

* Crear un repositorio en <https://github.com/acortiz-lang/Reforma_loft/edit/main/>

* Crear un proyecto en Rstudio 

* Crear un documento en formato Rmarkdown

* Tejer el documento Markdown en formato web (htlm)

* Descargar los archivos de Rstudio

* Renombrar el archivo htlm como index.htlm (en mi computadora)

* Subir los archivos a github


